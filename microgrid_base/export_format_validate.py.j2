from ies_optim import {{",".join(...) }}
{# import device ports. #}
from typing import List

{% for k in ['出力曲线', '']%}
class 设备出力曲线(BaseModel):
    元件名称:str
    时间:List[int]
    """
    单位：one
    (根据具体计算步长确定)
    """

{% for ... in ... %}
class {{...}}出力曲线(设备出力曲线):
{% endfor%}

class 仿真结果(BaseModel):
    元件名称:str
    设备型号:str
    设备台数:int

{% for ... in ... %}
class {{...}}仿真结果(仿真结果):
{% endfor%}