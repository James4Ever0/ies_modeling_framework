from ies_optim import {{",".join(...) }}
{# import device ports. #}
from typing import List

{% for k in ['出力曲线', '仿真结果']%}
{% if k == "出力曲线"%}
    {% set x = "设备"+k%}
{% else%}
    {% set x = k%}
{% endif%}
class {{x}}(BaseModel):
    元件名称:str
    {% if k == "出力曲线"%}
    时间:List[int]
    """
    单位：one
    (根据具体计算步长确定)
    """
    {% else%}
    设备型号:str
    设备台数:int
    """
    单位：one
    """
    {% endif%}


{% for ... in ... %}
class {{...}}{{k}}({{x}}):
{% endfor%}

class 仿真结果(BaseModel):
    元件名称:str
{% for ... in ... %}
class {{...}}仿真结果(仿真结果):
{% endfor%}