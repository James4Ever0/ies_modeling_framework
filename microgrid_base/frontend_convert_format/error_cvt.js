const error_mapping = {
    
}
const reserved_error_msg = '其他错误'
function convertErrorMsg(error_msg){
    var splited_lines = error_msg.split("\n")
    var error_last_line = splited_lines[splited_lines.length-1]

    return converted_error_msg
}